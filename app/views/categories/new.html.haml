= form_with(model: @category, locale: true) do |f|




  = f.text_field :name

  = f.collection_select :parent_id, Category.roots.order(:name), :id, :name, include_blank: true

  = f.submit "Add category"